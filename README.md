# Grape simplest API

This is a Grape API using Rack and MiniTest.

## Usage

#### Run tests:

```bash
$ rake
```

#### Run server:

```bash
$ rackup
```
Then go to <http://localhost:9292/api/v0/hello>
